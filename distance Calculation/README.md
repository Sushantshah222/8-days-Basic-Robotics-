# Distance Calculation

Example code for measuring distance with an ultrasonic sensor. The sketch lives in the `ultrasonic_sensor` folder.

## Usage
1. Open `ultrasonic_sensor/ultrasonic_sensor.ino` in the Arduino IDE.
2. Upload it to your board and open the Serial Monitor to view the measured distance.
