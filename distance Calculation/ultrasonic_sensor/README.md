# Ultrasonic Sensor Example

Reads the distance to an object using a standard HC‑SR04 ultrasonic sensor and prints the value over serial at 9600 baud.

Connect the trigger pin to pin `10` and the echo pin to `13` as defined in the sketch.
