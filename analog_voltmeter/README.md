# Analog Voltmeter

This project demonstrates how to read an analog voltage on an Arduino and display the value on the serial monitor.

## Usage
1. Open `analog_voltmeter.ino` in the Arduino IDE.
2. Upload the sketch to your board (e.g. Arduino Uno).
3. Open the Serial Monitor at `9600` baud to view the voltage values measured on pin `A4`.
